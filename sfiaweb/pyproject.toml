[project]
name = "sfiaweb"
version = "0.1.0"
description = "Servidor de Interface e Relatórios VC"
requires-python = "==3.12.*"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]

[tool.uv]
managed = true