[project]
name = "sqlite-viewer-py"
version = "0.1.0"
description = "Add your description here"
requires-python = "==3.12.*"
authors = [
    { name = "pmarote" }
]
dependencies = [
    "pandas>=2.3.3",
    "streamlit>=1.55.0",
]

[tool.uv]
managed = true