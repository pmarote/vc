[project]
name = "vc-utils"
version = "0.1.0"
description = "Toolkit de suporte e utilitários compartilhados"
requires-python = "==3.12.*"
dependencies = [
    "requests",
    "beautifulsoup4",
    "httpx"
]

[tool.uv]
managed = true